
import { api, firstErrorMessage, formatCurrency, restoreSession } from "../api.js";
const panel=document.getElementById("payment-panel");const params=new URLSearchParams(location.search);const payment=params.get("payment");const token=params.get("token");function esc(v){const d=document.createElement("div");d.textContent=v??"";return d.innerHTML;}async function load(){await restoreSession();try{const data=await api.get(`/payments/${payment}/?token=${encodeURIComponent(token)}`);panel.innerHTML=`<div class="payment-brand">AMBLE × PayShap</div><p class="eyebrow">Local development simulator</p><h1>Approve payment request</h1><p>This screen simulates the banking-app approval step. No money moves.</p><div class="payment-amount">${formatCurrency(data.amount)}</div><div class="payment-actions"><button class="btn btn--primary" data-outcome="successful">Approve payment</button><button class="btn btn--secondary" data-outcome="failed">Simulate failure</button><button class="btn btn--ghost" data-outcome="cancelled">Cancel</button></div><p class="form-error" id="error"></p>`;panel.querySelectorAll("[data-outcome]").forEach((button)=>button.addEventListener("click",()=>complete(button.dataset.outcome)));}catch(e){panel.innerHTML=`<h1>Payment unavailable</h1><p>${esc(firstErrorMessage(e))}</p>`;}}async function complete(outcome){panel.querySelectorAll("button").forEach((b)=>b.disabled=true);try{await api.post(`/payments/${payment}/mock-complete/`,{outcome,order_token:token});location.href=`/payment-return.html?payment=${encodeURIComponent(payment)}&token=${encodeURIComponent(token)}`;}catch(e){document.getElementById("error").textContent=firstErrorMessage(e);panel.querySelectorAll("button").forEach((b)=>b.disabled=false);}}load();
